spring.application.name=otaku-shop

# Server
server.port=8080
server.servlet.context-path=/api

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/otaku_shop
spring.datasource.username=postgres
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
# Para desarrollo, permitir que Hibernate cree/actualice las tablas
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Flyway - Deshabilitado por ahora, Hibernate crea las tablas
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# Logging
logging.level.root=INFO
logging.level.com.otakushop=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT
jwt.secret=otakushop-secret-key-very-long-and-secure-for-production-use-only
jwt.expiration=86400000

# CORS
cors.allowedOrigins=http://localhost:5173,http://localhost:3000,http://localhost:4173
cors.allowedMethods=GET,POST,PUT,DELETE,OPTIONS
cors.allowedHeaders=*
cors.allowCredentials=true

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=noreply@otakushop.com
app.frontend.url=http://localhost:5173

# Redis Configuration (opcional - comentado por defecto)
#spring.data.redis.host=localhost

# File Upload Configuration
file.upload.path=uploads/images
file.upload.max-size=5242880
#spring.data.redis.port=6379
#spring.cache.type=redis
#spring.cache.redis.time-to-live=600000

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Task Scheduling
spring.task.scheduling.pool.size=5

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# OAuth2 Configuration
# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=80909795587-4mkb8b720dvsna7ujc1ol26kdq7fblkd.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# Facebook OAuth2
spring.security.oauth2.client.registration.facebook.client-id=YOUR_FACEBOOK_APP_ID
spring.security.oauth2.client.registration.facebook.client-secret=YOUR_FACEBOOK_APP_SECRET
spring.security.oauth2.client.registration.facebook.scope=email,public_profile
spring.security.oauth2.client.registration.facebook.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# OAuth2 Redirect URI (frontend)
app.oauth2.redirectUri=http://localhost:5174/oauth2/redirect
